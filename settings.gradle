pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Warning: this repository is going to shut down soon
        maven {
            allowInsecureProtocol true
            url "http://localhost:8081/repository/myself_hosted/"
            credentials{
                username "admin"
                password "123456"
            }
        }
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "MavenProject"
include ':app'
include ':mylibrary'
include ':mylibrary2'
